<!--index.wxml-->
<!-- <view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
 -->        
  <!-----------------LIST AND MAP TABBAR---------->
<view class="weui-tab">
    <view class="weui-navbar">
        <block wx:for="{{tabs}}" wx:key="*this">
            <view id="{{index}}" class="weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}" bindtap="tabClick">
                <view class="weui-navbar__title">{{item}}</view>
            </view>
        </block>
        <view class="weui-navbar__slider" style="left: {{sliderLeft}}px; transform: translateX({{sliderOffset}}px); -webkit-transform: translateX({{sliderOffset}}px);"></view>
    </view>
    <!-------------List---------->
    <view class="weui-tab__panel">
    <view class="weui-tab__content" hidden="{{activeIndex != 0}}">

    <!-----------Scrolling left and right Filters--------->
      <view class="filters">
        <view class="selected-type">Type</view>
        <view class="type">Type</view>
        <view class="type">Type</view>
      </view>

    <!--------Popular--------->
     <!-- <text class="cards-text">Popular</text>
     <view class="popular-cards">
    <view class="popular-card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
        </view>
        <view class="popular-card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
        </view>
      <view class="popular-card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
        </view>
        </view> -->

    <!-----------Cards--------->
        <view class="cards">
        <!-- <text class="cards-text">All</text> -->
          <block wx:for="{{skatespots}}" wx:for-item="skatespot" >
            <view class="card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
              <view class="card-description">
                  <text class="distance">Distance</text>
              </view>
            </view>
            <!-- <view class="card-footer">
              <text class='name'>INFORMATION</text>
              <view class="pending">
              <text class="status-pending">Pending</text>
              </view>
            </view> -->
          </block>
      </view>

<!--to be deleted after made dynamic, just for purpose of layout--->
      <view class="card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
          <view class="card-description">
              <text class="distance">Distance</text>
          </view>
        </view>

       <view class="card" bindtap='showMachine' data-id="{{skatespot.id}}" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('https://picsum.photos/200/300/?random');">
          <view class="card-description">
              <text class="distance">Distance</text>
          </view>
        </view>
    </view>
  </view>
</view>

            <!----------------Map---------->
      <view class="weui-tab__content" hidden="{{activeIndex != 1}}">
      </view>
      